# CLI Dojo webterm tmux config
#
# Keep Ctrl+B free for terminal app usage and automation.
set -g prefix C-a
unbind C-b
bind C-a send-prefix

# Stable behavior for screenshot automation.
set -g status off
set -g mouse off
